# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

plugin 'cocoapods-keys', {
  :project => "home-assistant-hotkeys",
  :keys => [
  "host",
  "secret"
  ]}

target 'home-assistant-hotkeys' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for wifiname
  pod 'Alamofire', '~> 4.8'
  pod 'SwiftyJSON', '~> 4.0'
  pod 'RealmSwift'
  pod 'HotKey'
  pod 'Preferences'
  pod 'LaunchAtLogin'
  
end
